import {opacity} from './colors';

const shadow = {
  shadowColor: opacity.shadowCardOpacity,
  shadowOffset: {
    height: 2,
    width: 2,
  },
  shadowOpacity: 1.0,
  shadowRadius: 12,
  elevation: 10,
};

export default shadow;
